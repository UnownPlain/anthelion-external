import{z as e}from"zod";let t=function(e){return e.Html=`html`,e.Markdown=`markdown`,e.PlainText=`plain-text`,e.Github=`github`,e.Json=`json`,e.Yaml=`yaml`,e.BrowserRendering=`browser-rendering`,e}({});const n=e.enum([`html`,`markdown`,`plain-text`]).describe(`Source format for nested release notes content. Supports html, markdown, and plain-text.`),r=e.object({source:e.literal(`html`),sourceUrl:e.string().describe(`Source URL to fetch and parse release notes from. Supports {version} placeholders in URL.`),releaseNotesUrl:e.string().describe(`Optional URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders in URL.`).optional(),characterLimit:e.int().positive().describe(`Optional max character limit for parsed HTML release notes.`).optional(),cleanup:e.boolean().default(!0).describe(`Cleanup release notes with AI. Enabled by default for HTML sources.`).optional()}),i=e.object({source:e.literal(`markdown`),sourceUrl:e.string().describe(`Source URL to fetch and parse markdown release notes from. Supports {version} placeholders in URL.`),releaseNotesUrl:e.string().describe(`Optional URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders in URL.`).optional(),characterLimit:e.int().positive().describe(`Optional max character limit for parsed markdown release notes.`).optional(),cleanup:e.boolean().default(!0).describe(`Cleanup release notes with AI. Enabled by default for markdown sources.`).optional()}),a=e.object({source:e.literal(`plain-text`),sourceUrl:e.string().describe(`Source URL to fetch plain text release notes from. Supports {version} placeholders in URL.`),releaseNotesUrl:e.string().describe(`Optional URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders in URL.`).optional(),characterLimit:e.int().positive().describe(`Optional max character limit for fetched plain text release notes.`).optional(),cleanup:e.boolean().default(!0).describe(`Cleanup release notes with AI. Disabled by default for plain text sources.`).optional()}),o=e.object({source:e.literal(`browser-rendering`),sourceUrl:e.string().describe(`Source URL to fetch rendered markdown release notes from using Cloudflare Browser Rendering. Supports {version} placeholders in URL.`),releaseNotesUrl:e.string().describe(`Optional URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders in URL.`).optional(),characterLimit:e.int().positive().describe(`Optional max character limit for parsed markdown release notes.`).optional(),waitUntil:e.enum([`load`,`domcontentloaded`,`networkidle0`,`networkidle2`]).describe(`Optional browser wait condition for navigation.`).optional(),waitForSelector:e.string().describe(`Optional selector to wait for before capturing markdown.`).optional(),cleanup:e.boolean().default(!0).describe(`Cleanup release notes with AI. Enabled by default for browser rendering sources.`).optional()}),s=e.object({source:e.literal(`github`),owner:e.string().describe(`GitHub repository owner. Optional when strategy is github-release (defaults to github.owner).`).optional(),repo:e.string().describe(`GitHub repository name. Optional when strategy is github-release (defaults to github.repo).`).optional(),tag:e.string().describe(`Tag template for the release notes lookup. Supports {version}. Optional when strategy is github-release (uses resolved release tag).`).optional(),cleanup:e.boolean().default(!1).describe(`Cleanup release notes with AI. Disabled by default for GitHub sources.`).optional()}),c=e.object({source:e.literal(`json`),sourceUrl:e.url().describe(`Endpoint returning JSON release notes content or URLs. Supports {version}.`),releaseNotesUrl:e.string().describe(`Optional URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders in URL.`).optional(),path:e.string().describe(`Dot-separated path to release notes content or URL.`),nestedSource:n,cleanup:e.boolean().describe(`Cleanup release notes with AI. Defaults to the nestedSource cleanup behavior.`).optional()}),l=e.object({source:e.literal(`yaml`),sourceUrl:e.url().describe(`Endpoint returning YAML release notes content or URLs. Supports {version}.`),releaseNotesUrl:e.string().describe(`Optional URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders in URL.`).optional(),path:e.string().describe(`Dot-separated path to release notes content or URL.`),nestedSource:n,cleanup:e.boolean().describe(`Cleanup release notes with AI. Defaults to the nestedSource cleanup behavior.`).optional()}),u=e.object({releaseNotesUrl:e.string().describe(`URL to set in the manifest ReleaseNotesUrl field. Supports {version} placeholders.`)}).strict(),d=e.discriminatedUnion(`source`,[r,i,a,o,s,c,l]),f=e.union([d,u]).optional();export{t as ReleaseNotesSource,f as releaseNotesSchema};