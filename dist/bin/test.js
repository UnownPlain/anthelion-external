#!/usr/bin/env bun
const e=process.argv.slice(2),t=e.indexOf(`--dry-run`);t!==-1&&(process.env.DRY_RUN=`true`,e.splice(t,1));const n=e.flatMap(e=>e.trim().split(` `));if(n.length===0)throw Error(`At least one package ID or "all" is required`);const{runAllShards:r}=await import(`./main.js`);await r(n.includes(`all`)?void 0:n)>0&&process.exit(1);export{};